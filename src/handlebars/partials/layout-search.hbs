<!DOCTYPE html>
<html lang="ko">
  {{> head}}
  <body class="before-load">
    {{> noscript}}
    {{> loading}}
    <div class="wrapper">
      {{> header}}
      {{> search}}
      <div class="container">
        <main class="contents">
          {{{innerHTML}}}
        </main>
      </div>
    </div>
    {{> footer}}
  </body>
</html>